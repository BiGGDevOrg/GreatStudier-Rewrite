<nav class="navbar bg-light navbar-expand-lg sticky-top p-1 shadow">
  <a class="navbar-brand px-2 text-primary" href="<%= root_path %>">
    <b>GreatStudier</b>
  </a>

  <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
          data-bs-target=".navbar-collapse" aria-controls="navbarToggle" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-between">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item item">
        <a class="nav-link text-primary" href="<%= root_path %>">Home</a>
      </li>
      <li class="nav-item item">
        <a class="nav-link text-primary" href="<%= card_sets_path %>">Explore Sets</a>
      </li>
      <li class="nav-item item">
        <a class="nav-link text-primary" href="https://github.com/BiGGDevOrg/GreatStudier-Rewrite" target="_blank">GitHub</a>
      </li>
    </ul>

    <ul class="navbar-nav ml-auto" style="margin-right: 15px;">
      <li class="nav-item item dropdown" style="margin-right: 10px;">
        <a class="dropdown btn btn-outline-primary dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
          New Set
        </a>
        <div class="dropdown-menu dropdown-menu">
          <a class="dropdown-item" href="<%= new_card_set_path %>">Create a New Set</a>
          <a class="dropdown-item" href="<%= new_upload_path %>">Upload a Set</a>
        </div>
      </li>

      <li class="nav-item item">
        <% if user_signed_in? %>
          <a class="btn btn-primary" href="<%= user_path(current_user.id) %>">Profile</a>
        <% else %>
          <a class="btn btn-primary" href="<%= new_user_session_path %>">Login</a>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
