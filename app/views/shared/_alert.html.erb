<% flash.each do |type, msg| %>
  <div class="alert alert-<%= type == "notice" ? "success" : type %> fade show alert-dismissible">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>